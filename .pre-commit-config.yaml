repos:
  # General file checks
- repo: https://github.com/pre-commit/pre-commit-hooks
  rev: v5.0.0
  hooks:
  - id: trailing-whitespace
    args: [--markdown-linebreak-ext=md]
  - id: end-of-file-fixer
    exclude: ^(.*\.pdf|.*\.png|.*\.jpg|.*\.svg)$
  - id: check-yaml
    exclude: ^mkdocs\.yml$
  - id: check-added-large-files
    args: [--maxkb=5000]
  - id: check-case-conflict
  - id: check-merge-conflict
  - id: check-toml
  - id: detect-private-key
  - id: mixed-line-ending
    args: [--fix=lf]

  # LaTeX specific checks
- repo: https://github.com/jonasbb/pre-commit-latex-hooks
  rev: v1.5.0
  hooks:
  - id: american-eg-ie
    exclude: ^(examples/|content/|overleaf/|lib/layout/omnilatex-document\.sty)
  - id: cleveref-capitalization
    exclude: ^(examples/|content/|overleaf/)
  - id: consistent-spelling
    args:
    - --emph=et al.
    - --emph=a priori
    - --emph=a posteriori
    exclude: ^(examples/|content/|overleaf/|doc/)
  - id: csquotes
    exclude: ^(examples/|content/|overleaf/|doc/)
  - id: ensure-labels-for-sections
    exclude: ^(content/|examples/|overleaf/|config/|doc/|tests/|lib/)
  - id: no-space-in-cite
  - id: tilde-cite
    exclude: ^(examples/|content/|overleaf/|doc/|tests/|lib/)
  - id: unique-labels
    exclude: ^(examples/|content/|overleaf/|doc/|tests/|lib/)

  # Python code formatting (for all project Python files)
- repo: https://github.com/psf/black
  rev: 25.1.0
  hooks:
  - id: black
    language_version: python3
    files: ^((tests|buildlib|scripts|tools|docker)/.*\.py|build\.py)$

  # Python import sorting
- repo: https://github.com/PyCQA/isort
  rev: 7.0.0
  hooks:
  - id: isort
    args: [--profile, black]
    files: ^((tests|buildlib|scripts|tools|docker)/.*\.py|build\.py)$

  # Python linting
- repo: https://github.com/PyCQA/flake8
  rev: 7.3.0
  hooks:
  - id: flake8
    args: [--max-line-length=100, '--extend-ignore=E203,W503']
    files: ^((tests|buildlib|scripts|tools|docker)/.*\.py|build\.py)$

  # Markdown linting
- repo: https://github.com/igorshubovych/markdownlint-cli
  rev: v0.38.0
  hooks:
  - id: markdownlint
    args: [--fix]
    exclude: ^extensions/

  # YAML formatting
- repo: https://github.com/macisamuele/language-formatters-pre-commit-hooks
  rev: v2.16.0
  hooks:
  - id: pretty-format-yaml
    args: [--autofix, --indent, '2']
    exclude: ^(\\.woodpecker\\.yml|\\.github/workflows/|mkdocs\\.yml)
    language_version: python3

  # Check for common LaTeX mistakes
- repo: local
  hooks:
  - id: check-latex-todos
    name: Check for TODO markers in LaTeX source files
    entry: grep -n "\\\\todo\|TODO\|FIXME"
    language: system
    files: \.tex$
    exclude: ^(content/|examples/|overleaf/)
    pass_filenames: true

  - id: run-pytest-fast
    name: Run fast pytest suite (no compilation)
    entry: pytest tests/ -q --timeout=30 -m "not slow" --tb=short
    language: system
    pass_filenames: false
    always_run: true
    verbose: true

  - id: check-lean-proofs
    name: Verify Lean 4 proofs compile (lake build)
    entry: bash -c "cd specs/proofs && lake build 2>&1 | tail -5 || echo 'Lean not available, skipping'"
    language: system
    pass_filenames: false
    always_run: true
    verbose: true

  # Spell checking (optional - requires aspell)
  # - repo: https://github.com/codespell-project/codespell
  #   rev: v2.2.6
  #   hooks:
  #     - id: codespell
  #       args:
  #         - --skip="*.bib,*.log,*.aux,*.out,*.fls,*.fdb_latexmk,*.synctex.gz"
  #         - --ignore-words-list=crate,tabel
