# Changelog v2.5.0 -- 2026-06-09

## Testing & Code Quality

### Added

- **346 new tests**: Total test count 1365 -> 1711 (buildlib 78% coverage, exceeds 72% target)
- **14 web vitest tests**: Utility functions and component tests

### Fixed

- **Dead code removed**: Unused i18n directory, PDF viewer CSS, CSS_COLORS dict, @tanstack/solid-virtual import
- **Code deduplication**: SSIM formula consolidated, build command construction centralized in config.py
- **Exception safety**: Narrowed except Exception to specific types in _compile_example_worker
- **Thread safety**: Replaced mutable timeout_flag with threading.Event
- **Cache atomicity**: Atomic writes (temp+rename) prevent corruption on crash
- **TOCTOU race**: PDF size captured once, not re-stat'd in finally block
- **Path correctness**: Cleanup uses absolute paths from REPO_ROOT
- **Test isolation**: builder.REPO_ROOT monkeypatch causing real filesystem side effects

## CI/CD

### Fixed

- **Action SHAs pinned**: All 30 GitHub Actions references pinned to commit SHAs
- **bc float comparison**: Fixed in lean4-ci.yml
- **Dependabot directories**: Corrected npm to /web, pip to /tests
- **Dead workflows removed**: _docker-setup.yml, setup-texlive composite action

## Web Frontend

### Added

- **SEO**: og:image/twitter:image meta tags for social sharing
- **Structured data**: JSON-LD SoftwareSourceCode schema in BaseLayout
- **Table of contents**: Nested h2/h3 TOC sidebar on doc pages with sticky positioning
- **Typography scale**: --text-xs through --text-3xl design tokens in global.css
- **Focus management**: Keyboard focus trapping on mobile nav, Escape-to-close

### Changed

- **Font sizes**: Converted hardcoded px values to rem design tokens
- **Theme consistency**: Replaced raw #fff with var(--fg)

### Removed

- **Dead code**: web/src/i18n/ directory, PDF viewer CSS classes

## VS Code Extension

### Fixed

- **Snippet syntax**: Fixed broken ${2|none} choice syntax to ${2:none}
- **Banner color**: Aligned galleryBanner with design system (#0A0E13)
- **Resource leak**: logDiagnostics now disposed in deactivate()

## Documentation

### Fixed

- **CTAN status**: FAQ.md updated from "not yet on CTAN" to published
- **Line count**: PART_I_FOUNDATIONS.md corrected from 2094 to 679 lines
- **Version accuracy**: CTAN_README, SECURITY.md, docs/index.md all updated to v2.5.0

## Roadmap

### Resolved

- All 21 post-audit items (#100-120) resolved: 17 FIXED, 2 DEFERRED, 1 N/A, 1 FIXED by dependency
