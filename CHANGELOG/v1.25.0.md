# Changelog

## [1.25.0] - 2026-05-11

### Added

- **`lib/graphics/omnilatex-beamer.sty`** -- Beamer integration applying OmniLaTeX colors, fonts, and clean layout to presentations
- **VS Code build-on-save** -- `omnilatex.buildOnSave` setting triggers latexmk on .tex save
- **pytest-cov in CI** -- build.yml now runs coverage with `--cov=tests --cov=build.py`, enforces 60% threshold

### Changed

- **tests/pyproject.toml** -- declared `pytest-cov = "^5.0"` dependency
- **.github/workflows/build.yml** -- added coverage flags and threshold enforcement step

### Verified

- 429 fast tests pass
- 154 Lean theorems across 15 modules, 0 sorry
- Incremental builds already fully implemented (source hash cache, --force flag)
- docs/OVERLEAF.md already exists (no changes needed)
