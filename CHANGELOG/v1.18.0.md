## [1.18.0] - 2026-05-10

### Added

- **Lean 4 proof-code consistency rewrite** (53 to 64 theorems)
  - `DocumentSettings.lean`: rewritten to match canonical DOCTYPE_TO_CLASS mapping
  - `DoctypeClassMapping.lean`: rewritten with correct alias-class counts, disjointness proofs
  - `I18nCompleteness.lean`: fixed norwegian to norsk
  - `LanguageFallback.lean`: replaced fictional secondary languages with 7 real polyglossia-only languages
- **Root-level `Makefile`** — test/test-fast/test-slow/test-lean/lint/format/clean/build targets
- **`.editorconfig`** — per-language indentation rules
- **`ROADMAP-DETAILED.md`** — phased roadmap
- **`test_i18n_language_coverage_matrix`** test
- **Pre-commit hooks** — run-pytest-fast and check-lean-proofs
- **`tests/pyproject.toml`** — hypothesis and pytest-timeout declared

### Changed

- **CI pipeline** — added `test_properties.py` and `test_visual_regression.py` to test job
- **`test_properties.py`** — deduplicated DOCTYPE_ALIASES
- **`VERSION.md`, `omnilatex.cls`, `build.lua`** — version bumped to v1.18.0

### Fixed

- **Documentation audit** — corrected stale counts across 16 files
- **Test timeouts** — edge_cases 120s to 180s, negative 60s to 180s, unicode 120s to 180s
- **`docker_required` fixture** — now verifies Docker can actually run containers
- **`run_module_tests.py`** — removed non-existent bibliography module test reference
- **Unused imports** — `os` from `test_edge_cases.py`, `pytest` from `test_negative.py`
- **CHANGELOG duplicate cleanup** — 3 duplicates from v1.13.0, 4 duplicates from v1.16.0
- **All test files** — black/isort formatting, flake8 lint compliance
